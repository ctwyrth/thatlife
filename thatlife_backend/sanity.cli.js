// Sanity CLI — project/dataset for Studio v3 local + deploy commands.
import {defineCliConfig} from 'sanity/cli'

export default defineCliConfig({
  api: {
    projectId: 'nnu4f987',
    dataset: 'production',
  },
})
